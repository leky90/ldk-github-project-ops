# LDK GitHub Project Ops

Use the four packaged skills for GitHub Project work. Keep this source plugin
project-agnostic: never commit a consumer `.github-project-ops.json`, credential,
repository-specific issue export, or runtime `.github-ops/` state.
